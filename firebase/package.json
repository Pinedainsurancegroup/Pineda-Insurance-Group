{
  "private": true,
  "scripts": {
    "test:rules": "firebase emulators:exec --only auth,firestore --project demo-pag-leads --config firebase.json 'node tests/rules.test.cjs && node tests/recruitment-sync.test.cjs && node tests/owner-password.test.cjs'"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.2",
    "firebase": "12.19.0",
    "firebase-tools": "15.31.0"
  }
}
